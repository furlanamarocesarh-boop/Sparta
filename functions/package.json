{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test \"lib-test/test/unit/*.test.js\"",
    "test:rules": "npm run build:test && firebase emulators:exec --project demo-sparta-battle --only firestore \"node --test lib-test/test/rules/*.test.js\"",
    "audit:data": "npm run build && node lib/audit/cli.js",
    "audit:data:emulator": "npm run build && firebase emulators:exec --project demo-sparta-battle --only firestore \"node lib/audit/cli.js --project demo-sparta-battle\"",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "//overrides": "Bump só os pacotes transitivos com advisory high de runtime para a última versão patch/minor da mesma major (não-breaking). grpc-js é escopado sob google-gax para não tocar o @grpc/grpc-js@1.9.x dev (não vulnerável). protobufjs em 7.6.5 (última 7.x) cobre GHSA-wcpc-wj8m-hjx6 (high, <=7.6.0) sem ir para a major 8. Direct deps inalteradas. Remover quando firebase-admin trouxer estas versões por conta própria.",
  "overrides": {
    "google-gax": {
      "@grpc/grpc-js": "1.14.4"
    },
    "form-data": "2.5.6",
    "protobufjs": "7.6.5"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "@types/node": "26.1.1",
    "firebase-functions-test": "^3.4.1",
    "typescript": "^6.0.0"
  },
  "private": true
}
